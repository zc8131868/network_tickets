# Project environment overrides (loaded by `network_tickets/settings.py`)
#
# Redis cache URL format:
#   redis://<host>:<port>/<db>
#
# Make sure a Redis SERVER is running at this address.
REDIS_URL=redis://127.0.0.1:6379/1

# Elasticsearch (EFK stack) — used by syslog search API
ELASTIC_URL=https://elasticsearch.qytopensource.com:9200
ELASTIC_USERNAME=elastic
ELASTIC_PASSWORD=Cmhk@94!
ELASTIC_CA_CERT=/it_network/open_source_software_3_EFK_NetFlow/certs/ca.cer
ELASTIC_INDEX_PATTERN=network-logs-*,cisco-logs-*,panw-logs-*
ELASTIC_MAX_RESULTS=200

# Bearer token for OpenClaw → Django internal API calls
OPENCLAW_INTERNAL_TOKEN=netcare-openclaw-syslog-2026

